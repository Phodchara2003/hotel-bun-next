# Staff Booking Detail Access - Complete

## สรุปการแก้ไขให้พนักงานสามารถดูข้อมูลการจองได้

### ✅ ปัญหาที่แก้ไขแล้ว:

#### 🔍 **การเข้าถึงรายละเอียดการจอง:**
- ✅ Staff สามารถกดปุ่ม "ดูรายละเอียด" ได้
- ✅ Staff สามารถเห็น Modal รายละเอียดการจองเต็มรูปแบบ
- ✅ Staff สามารถดูข้อมูลลูกค้าและการจองได้

#### 📋 **ข้อมูลที่ Staff เห็นได้ใน Modal:**
- ✅ **ข้อมูลการจอง:** รหัสการจอง, สถานะ, วันที่เข้า-ออก
- ✅ **ข้อมูลโรงแรม:** ชื่อโรงแรม, ที่อยู่, ประเภทห้อง
- ✅ **ข้อมูลลูกค้า:** ชื่อ, อีเมล, เบอร์โทร, ที่อยู่
- ✅ **ข้อมูลการชำระเงิน:** หลักฐานการชำระเงิน (ถ้ามี)
- ✅ **คำขอพิเศษ:** ข้อความพิเศษจากลูกค้า

#### 🎛️ **สิทธิ์ในการจัดการ Modal:**
- ✅ **Staff เห็น:** ปุ่ม "ยืนยัน" และ "ยกเลิก" สำหรับ pending booking
- ✅ **Staff เห็น:** ปุ่ม "ไปหน้าชำระเงิน" และ "ยกเลิก" สำหรับ confirmed booking
- ❌ **Staff ไม่เห็น:** ปุ่ม "อนุมัติ" (เฉพาะ Admin)

### 🔧 Technical Implementation:

#### **Backend Changes:**

1. **New Admin API Endpoint:**
```javascript
GET /api/bookings/admin/:id
// รองรับ staff และ admin
// ข้อมูลเต็มรูปแบบรวมถึงข้อมูลลูกค้าและโรงแรม
```

2. **Updated Route Protection:**
```javascript
// เดิม: เฉพาะ booking owner หรือ admin
WHERE b.id = ${bookingId} AND (b.user_id = ${user.id} OR ${user.role === 'admin'})

// ใหม่: รองรับ staff
WHERE b.id = ${bookingId} AND (b.user_id = ${user.id} OR ${['admin', 'staff'].includes(user.role)})
```

#### **Frontend Changes:**

1. **New API Function:**
```javascript
getAdminBookingById: async (id) => {
  const response = await api.get(`/bookings/admin/${id}`);
  return response.data;
}
```

2. **Updated Dashboard:**
```javascript
// ใช้ admin API แทนของ user
const response = await bookingAPI.getAdminBookingById(bookingId);
```

3. **Permission-Based UI:**
```javascript
// ปุ่มยืนยัน/ยกเลิก - Staff + Admin
{selectedBooking.status === 'pending' && canManageBookings(user) && (...)}

// ปุ่มอนุมัติ - Admin เท่านั้น  
{canApproveBookings(user) && (...)}
```

### 📱 Staff User Experience:

#### **ขั้นตอนการใช้งาน:**
1. เข้าสู่ระบบด้วย `staff@royalgarden.com`
2. ไปหน้า Admin Dashboard
3. เห็นรายการการจองทั้งหมด
4. **กดปุ่ม "ดูรายละเอียด"** → เปิด Modal รายละเอียด
5. **เห็นข้อมูลครบถ้วน:** ลูกค้า, โรงแรม, การชำระเงิน
6. **สามารถจัดการ:** ยืนยัน/ยกเลิกการจองจาก Modal

#### **ข้อมูลที่ Staff เข้าถึงได้:**
- 📊 **ข้อมูลการจอง**: รหัสอ้างอิง, สถานะ, วันที่, จำนวนแขก, ราคารวม
- 🏨 **ข้อมูลโรงแรม**: ชื่อ, ที่อยู่, ประเภทห้อง, สิ่งอำนวยความสะดวก, ราคาต่อคืน
- 👤 **ข้อมูลลูกค้า**: ชื่อ, อีเมล, เบอร์โทร, ที่อยู่, เลขบัตรประจำตัว
- 💳 **ข้อมูลการชำระเงิน**: หลักฐานการชำระเงิน (รูปภาพ/ไฟล์)
- 📝 **คำขอพิเศษ**: ข้อความเพิ่มเติมจากลูกค้า
- 📅 **ข้อมูลระบบ**: วันที่สร้าง, วันที่อัปเดตล่าสุด

#### **การจัดการจาก Modal:**
- ✅ **Pending Booking**: ยืนยัน → confirmed, ยกเลิก → cancelled
- ✅ **Confirmed Booking**: ไปหน้าชำระเงิน, ยกเลิก → cancelled
- ❌ **ไม่สามารถ**: อนุมัติให้เป็น completed (เฉพาะ admin)

### 🎯 การใช้งานจริง:

**Staff สามารถ:**
- ✅ ดูรายละเอียดการจองทั้งหมดได้เหมือน Admin
- ✅ เข้าใจข้อมูลลูกค้าและสถานะการจองได้ชัดเจน
- ✅ ช่วยตอบคำถามลูกค้าเกี่ยวกับการจองได้
- ✅ จัดการการจองขั้นพื้นฐานได้ (ยืนยัน/ยกเลิก)
- ✅ ดูหลักฐานการชำระเงินได้

**แต่ไม่สามารถ:**
- ❌ อนุมัติการจองให้เสร็จสิ้น (completed)
- ❌ ลบการจองออกจากระบบ
- ❌ แก้ไขข้อมูลการจองโดยตรง

### ✅ System Ready:
ระบบพร้อมให้พนักงานดูข้อมูลการจองได้อย่างครบถ้วนแล้ว! Staff สามารถใช้งานได้เหมือน Admin แต่มีข้อจำกัดที่เหมาะสม เพื่อความปลอดภัยของข้อมูลและการควบคุมการดำเนินงาน
