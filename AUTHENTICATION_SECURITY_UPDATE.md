# 🔒 แก้ไขระบบ Authentication - บังคับล็อกออกเมื่อเข้าหน้าแรก

## ✅ การเปลี่ยนแปลงที่ทำ

### 1. ปรับปรุง AuthContext.jsx
- **ลด session timeout** จาก 7 วัน เหลือ 6 ชั่วโมง (0.25 วัน)
- **เปลี่ยนจาก localStorage เป็น sessionStorage** เพื่อให้ล็อกออกเมื่อปิดเบราว์เซอร์
- **เพิ่มการล้าง sessionStorage** ทั้งหมดเมื่อล็อกออก
- **ส่งค่า user data กลับใน login result** สำหรับการตรวจสอบ role

### 2. แก้ไขหน้าแรก (Homepage)
- **บังคับล็อกออกอัตโนมัติ** เมื่อเข้าหน้าแรก
- **แสดงข้อความแจ้งเตือน** ให้ล็อกอินใหม่เพื่อความปลอดภัย
- **ไม่ redirect admin ไปหน้า dashboard อัตโนมัติ**

### 3. ปรับปรุง Middleware
- **เช็ค token และ user data อย่างเข้มงวด**
- **ตรวจสอบ role ว่าเป็น admin จริง** ก่อนให้เข้า admin panel
- **Redirect ไปหน้า login พร้อม redirect parameter**
- **ป้องกันการเข้า login/register เมื่อล็อกอินแล้ว**

### 4. แก้ไขหน้า Login
- **Redirect admin ไป dashboard**
- **Redirect user ธรรมดาไป bookings page** แทนหน้าแรก
- **เพิ่ม delay เพื่อให้ state update เสร็จก่อน redirect**

## 🔐 วิธีการทำงานใหม่

### สำหรับผู้ใช้ทั่วไป:
1. เข้าหน้าแรก → บังคับล็อกออกอัตโนมัติ
2. ต้องกดปุ่ม "เข้าสู่ระบบ" เพื่อล็อกอิน
3. ล็อกอินสำเร็จ → ไปหน้า bookings
4. Session หมดอายุใน 6 ชั่วโมง
5. ปิดเบราว์เซอร์ → ล็อกออกอัตโนมัติ

### สำหรับ Admin:
1. เข้าหน้าแรก → บังคับล็อกออกอัตโนมัติ
2. ต้องกดปุ่ม "เข้าสู่ระบบ" เพื่อล็อกอิน
3. ล็อกอินสำเร็จ → ไปหน้า admin dashboard
4. พยายามเข้า admin panel โดยไม่ล็อกอิน → redirect ไป login
5. Session หมดอายุใน 6 ชั่วโมง
6. ปิดเบราว์เซอร์ → ล็อกออกอัตโนมัติ

## 🚀 Server URLs ใหม่
- **Frontend**: http://localhost:3004 (เปลี่ยนจาก 3003)
- **Backend**: http://localhost:3002 (เหมือนเดิม)

## 🧪 วิธีทดสอบ
1. เปิด http://localhost:3004
2. ตรวจสอบว่าไม่มีสิทธิ์ admin แสดงใน header
3. กดปุ่ม "เข้าสู่ระบบ"
4. ล็อกอินด้วย admin@hotel.com / password123
5. ควรไป admin dashboard อัตโนมัติ
6. กดปุ่ม "หน้าแรก" → ควรล็อกออกและกลับมาหน้าแรกแบบผู้ใช้ธรรมดา

## ✅ ปัญหาที่แก้แล้ว
- ❌ ไม่มีการเข้าระบบแบบค้างไว้แล้ว
- ❌ ไม่สามารถเข้า admin panel โดยไม่ล็อกอินแล้ว
- ✅ บังคับล็อกออกเมื่อเข้าหน้าแรก
- ✅ Session timeout ที่สั้นลง (6 ชั่วโมง)
- ✅ ล็อกออกเมื่อปิดเบราว์เซอร์
- ✅ ความปลอดภัยที่ดีขึ้น

ระบบตอนนี้ปลอดภัยและบังคับให้ล็อกอินใหม่ทุกครั้งแล้ว! 🎉
