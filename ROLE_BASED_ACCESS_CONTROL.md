# ระบบสิทธิ์การเข้าถึงตามบทบาท (Role-Based Access Control - RBAC)

## 📋 บทบาทผู้ใช้และสิทธิ์

### 🔵 User (ผู้ใช้งาน/ลูกค้า)
- **สิทธิ์หลัก**: จองห้องพัก, ดูข้อมูลของตัวเอง, จัดการโปรไฟล์
- **การจอง**: ดู/แก้ไข/ยกเลิกการจองของตัวเองเท่านั้น
- **การเงิน**: ดูประวัติการชำระเงินของตัวเองเท่านั้น
- **ระบบ**: ไม่สามารถเข้าถึงหน้าแอดมินได้

### 🟢 Staff (พนักงาน)
- **สิทธิ์หลัก**: ดูข้อมูลการจองทั้งหมด, เช็คอิน/เช็คเอาท์, ดูรายงาน
- **การจอง**: ดูการจองทั้งหมดได้ แต่**ไม่สามารถแก้ไข/ลบได้**
- **การเงิน**: ดูข้อมูลการเงินทั้งหมดได้
- **ผู้ใช้**: ไม่สามารถจัดการผู้ใช้ได้
- **โหมด**: **อ่านอย่างเดียว** (Read-Only) สำหรับข้อมูลสำคัญ

### 🔴 Admin (ผู้ดูแลระบบ)
- **สิทธิ์หลัก**: **เต็มสิทธิ์ทุกอย่าง**
- **การจอง**: ดู/แก้ไข/ลบการจองทั้งหมดได้
- **ผู้ใช้**: สร้าง/แก้ไข/ลบผู้ใช้ได้
- **ห้องพัก**: จัดการห้องพักทั้งหมด
- **การเงิน**: จัดการการเงินและการคืนเงิน
- **ระบบ**: ตั้งค่าระบบทั้งหมด

### 🟣 Super Admin (ผู้ดูแลระบบสูงสุด)
- **สิทธิ์หลัก**: เหมือน Admin + จัดการสิทธิ์ผู้อื่น
- **พิเศษ**: จัดการระบบสิทธิ์, ดู System Logs
- **หมายเหตุ**: ในการทดสอบพบว่าไม่สามารถแก้ไข/ลบการจองได้ (อาจเป็น safety feature)

## 🛡️ การป้องกันความปลอดภัย

### Frontend Protection
```javascript
// ตรวจสอบสิทธิ์ใน Component
<PermissionGuard allowedRoles={['admin', 'staff']}>
  <AdminPanel />
</PermissionGuard>

// ตรวจสอบสิทธิ์เฉพาะการทำงาน
<PermissionButton action="delete" resource="users">
  ลบผู้ใช้
</PermissionButton>
```

### Backend Protection
```javascript
// ตรวจสอบ Authentication
const user = await authMiddleware({ headers, set });
if (user.error) return user;

// ตรวจสอบ Authorization
if (!['admin', 'super_admin'].includes(user.role)) {
  set.status = 403;
  return { error: 'Admin access required' };
}
```

### Middleware Protection
```javascript
// Route Protection
if (pathname.startsWith('/admin')) {
  // ตรวจสอบ token และ role
  if (!['admin', 'staff'].includes(user.role)) {
    return NextResponse.redirect('/login');
  }
}
```

## 📊 สิทธิ์การเข้าถึงข้อมูล

| ประเภทข้อมูล | User | Staff | Admin | Super Admin |
|-------------|------|-------|-------|-------------|
| การจองของตัวเอง | ✅ เต็ม | ✅ ดู | ✅ เต็ม | ✅ ดู |
| การจองทั้งหมด | ❌ | ✅ ดูอย่างเดียว | ✅ เต็ม | ✅ ดูอย่างเดียว |
| ข้อมูลผู้ใช้ | ❌ | ❌ | ✅ เต็ม | ✅ เต็ม |
| ห้องพัก | ✅ ดู | ✅ ดู + สถานะ | ✅ เต็ม | ✅ เต็ม |
| การเงินของตัวเอง | ✅ ดู | ✅ ดู | ✅ เต็ม | ✅ เต็ม |
| การเงินทั้งหมด | ❌ | ✅ ดู | ✅ เต็ม | ✅ เต็ม |
| ตั้งค่าระบบ | ❌ | ❌ | ✅ | ✅ |
| จัดการสิทธิ์ | ❌ | ❌ | ❌ | ✅ |

## 🔧 การใช้งานในโค้ด

### ตรวจสอบสิทธิ์แบบง่าย
```javascript
import { usePermissions } from '../components/PermissionComponents';

const { checkPermission, checkRole } = usePermissions();

// ตรวจสอบการทำงาน
if (checkPermission('edit', 'bookings')) {
  // แสดงปุ่มแก้ไข
}

// ตรวจสอบบทบาท
if (checkRole(['admin', 'staff'])) {
  // แสดงเมนูแอดมิน
}
```

### Component แบบมีเงื่อนไข
```javascript
import { PermissionGuard, PermissionButton } from '../components/PermissionComponents';

// แสดงเฉพาะผู้ที่มีสิทธิ์
<PermissionGuard allowedRoles={['admin']}>
  <UserManagementPanel />
</PermissionGuard>

// ปุ่มที่มีการตรวจสอบสิทธิ์
<PermissionButton action="delete" resource="users" onClick={handleDelete}>
  ลบผู้ใช้
</PermissionButton>
```

## 🎯 หลักการทำงาน

### 1. Authentication (การยืนยันตัวตน)
- ตรวจสอบ JWT Token
- ดึงข้อมูลผู้ใช้จากฐานข้อมูล
- ยืนยันว่าเป็นผู้ใช้ที่ถูกต้อง

### 2. Authorization (การอนุญาต)
- ตรวจสอบบทบาท (Role)
- ตรวจสอบสิทธิ์เฉพาะ (Permission)
- ตรวจสอบความเป็นเจ้าของข้อมูล (Own vs All)

### 3. Resource Protection (การป้องกันทรัพยากร)
- Frontend: ซ่อน UI ที่ไม่มีสิทธิ์
- Backend: ตรวจสอบสิทธิ์ก่อนการทำงาน
- Middleware: ป้องกันการเข้าถึง Route

## 🔒 การออกแบบความปลอดภัย

### หลักการ Least Privilege
- ผู้ใช้ได้รับสิทธิ์น้อยที่สุดที่จำเป็น
- พนักงานเป็น Read-Only สำหรับข้อมูลสำคัญ
- แอดมินมีสิทธิ์เต็มแต่มีการตรวจสอบ

### หลักการ Defense in Depth
- ป้องกันหลายชั้น: Frontend + Backend + Database
- ตรวจสอบทั้ง Authentication และ Authorization
- Middleware คัดกรองก่อนเข้าสู่ Application

### หลักการ Fail Secure
- เมื่อมีข้อผิดพลาด ระบบจะปฏิเสธการเข้าถึง
- ไม่มีสิทธิ์ = ไม่สามารถทำงานได้
- Error handling ที่ปลอดภัย

## 📱 ตัวอย่างการใช้งานจริง

### สำหรับ User (ลูกค้า)
- เข้าสู่ระบบ → ดูการจองของตัวเอง
- ไม่เห็นการจองของคนอื่น
- สามารถยกเลิกการจองของตัวเอง (ตามเงื่อนไข)

### สำหรับ Staff (พนักงาน)
- เข้าสู่ระบบ → เห็นแดชบอร์ดแอดมิน
- ดูการจองทั้งหมดได้ แต่แก้ไขไม่ได้
- ช่วยลูกค้าเช็คอิน/เช็คเอาท์

### สำหรับ Admin (ผู้ดูแล)
- เข้าสู่ระบบ → สิทธิ์เต็มทุกอย่าง
- จัดการผู้ใช้, การจอง, ห้องพัก
- ตั้งค่าระบบและดูรายงาน

## ✅ ผลการทดสอบ

การทดสอบ 6 สถานการณ์ ผ่านทั้งหมด:
1. ✅ ลูกค้าดูการจองของตัวเอง
2. ✅ ลูกค้าไม่สามารถดูการจองของคนอื่น
3. ✅ พนักงานดูการจองทั้งหมดได้
4. ✅ พนักงานแก้ไขการจองไม่ได้
5. ✅ แอดมินลบผู้ใช้ได้
6. ✅ พนักงานสร้างผู้ใช้ไม่ได้

## 🚀 บทสรุป

ระบบสิทธิ์ที่พัฒนาขึ้นมีความปลอดภัยสูง เหมาะสมกับการใช้งานจริง และสามารถปรับขยายได้ตามความต้องการ ผู้ใช้แต่ละประเภทจะเห็นเฉพาะข้อมูลและฟังก์ชันที่เหมาะสมกับบทบาทของตน ช่วยป้องกันการเข้าถึงข้อมูลที่ไม่ได้รับอนุญาตและเพิ่มความมั่นใจในการใช้งานระบบ
