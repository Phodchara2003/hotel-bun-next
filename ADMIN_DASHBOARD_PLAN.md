# 📊 ระบบรายงานและ Dashboard

## ฟีเจอร์ที่ควรมีใน Admin Dashboard

### 📈 1. หน้า Overview Dashboard
- สถิติการจองรวม (วันนี้, สัปดาห์นี้, เดือนนี้)
- รายได้รวม และเปรียบเทียบกับเดือนก่อน
- จำนวนลูกค้าใหม่
- อัตราการจองเต็ม (Occupancy Rate)
- กราฟแสดงแนวโน้มการจอง

### 📊 2. รายงานการจอง
- รายการการจองทั้งหมด (มีฟิลเตอร์)
- สถานะการจอง (Confirmed, Cancelled, Pending)
- การจองตามประเภทห้อง
- การจองตามช่วงเวลา

### 💰 3. รายงานรายได้
- รายได้รายวัน/เดือน/ปี
- รายได้แยกตามโรงแรม
- รายได้แยกตามประเภทห้อง
- เปรียบเทียบรายได้ช่วงเวลาต่างๆ

### 👥 4. รายงานลูกค้า
- จำนวนลูกค้าทั้งหมด
- ลูกค้าใหม่ในแต่ละเดือน
- ลูกค้าที่จองบ่อยที่สุด
- อัตราการกลับมาจองซ้ำ

### 🏨 5. รายงานโรงแรม
- โรงแรมยอดนิยม
- อัตราการจองของแต่ละโรงแรม
- คะแนนรีวิวเฉลี่ย
- การเปรียบเทียบประสิทธิภาพ

## การติดตั้ง

1. สร้างหน้า `/admin/dashboard`
2. เพิ่ม API endpoints สำหรับดึงข้อมูลสถิติ
3. ใช้ Chart.js หรือ Recharts สำหรับกราฟ
4. เพิ่มระบบ Export เป็น PDF/Excel

## ตัวอย่าง API ที่ต้องสร้าง

```javascript
// GET /api/admin/stats/overview
// GET /api/admin/stats/bookings
// GET /api/admin/stats/revenue  
// GET /api/admin/stats/customers
// GET /api/admin/stats/hotels
```

## UI Components ที่ต้องสร้าง

- StatCard (แสดงตัวเลขสำคัญ)
- LineChart (แสดงแนวโน้ม)
- BarChart (เปรียบเทียบข้อมูล)
- DataTable (แสดงรายการ)
- DateRangePicker (เลือกช่วงเวลา)

## ความปลอดภัย

- ตรวจสอบสิทธิ์แอดมินก่อนเข้าถึง
- Rate limiting สำหรับ API calls
- Audit log สำหรับการเข้าถึงข้อมูล
